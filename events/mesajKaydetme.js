import Message from "../models/Message.js";

export default {
    name: "messageCreate",
    async execute(client, message) {
        /*
        const mesaj = new Message({
            content: message.content,
            userId: message.author.id
        });

        await mesaj.save();
        */

    },
};